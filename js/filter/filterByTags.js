const tagsList = document.getElementById('search-tags-list')

function filterByTags(tag, list) {
  switch (list) {
    case ingredient:
      break

    case ustensil:
      console.log()
      break

    case appliance:
      console.log()
      break

    default:
      console.log('the function parameters are not respected')
      break
  }
}
